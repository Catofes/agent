# Skill：函数与图像探究

## 工作步骤
1. 明确函数定义域、参数范围和要研究的图像特征。
2. 先从解析式预测对称性、周期、零点、单调性、极值和渐近行为。
3. 选择有意义的取值范围与采样密度，再使用 Python 绘图核验预测。
4. 改变一个参数，比较图像变化并用代数关系解释。
5. 对交点、切线、面积等观察给出解析或数值核验。
6. 区分屏幕窗口造成的视觉现象和函数本身的性质。

## 输出约束
- 坐标轴、图例和关键点要标注清楚。
- 不以图像代替严格证明，也不根据低分辨率图像断言精确值。
- 图像文件写入 `/workspace/output`。
