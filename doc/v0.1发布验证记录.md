# v0.1 发布验证记录

## 2026-08-06 Linux amd64

- 构建命令：`make release`
- 目标：`linux/amd64`
- 产物：`build/classroom-agent-linux-amd64`
- 文件类型：64 位 x86-64 ELF，静态链接，已 strip
- 构建版本：`cbed4cc`
- SHA-256：`5bb76f4cd98154981105a73c0a4b6117a75d0ac1778239a532efd6869e3e2d36`
- 校验：在 `build/` 中执行 `sha256sum -c classroom-agent-linux-amd64.sha256`，结果为 `OK`。

干净目录验证：

1. 新建临时空目录，只复制发布二进制和由 `data/students.example.csv` 得到的 `data/students.csv`。
2. 通过环境变量传入占位 DeepSeek Key、教师口令和匿名 HMAC 密钥；首次启动自动创建 SQLite 文件、迁移和初始场次。
3. `GET /healthz` 返回 `{"ok":true,"version":"cbed4cc"}`。
4. `GET /` 正常返回内嵌学生页面。
5. 发送 `SIGINT` 后服务立即优雅退出。

> 占位 Key 仅用于验证启动，过程没有发起任何真实模型请求。发布前应从干净提交重新构建并更新校验和。
