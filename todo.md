# 课堂 Agent 平台开发 TODO

> 依据：[课堂 Agent 平台 · 初步设计方案](doc/课堂Agent平台_初步设计方案.md)
>
> 目标：先交付可稳定用于 50 人公开课的 **v0.1**。所有任务按依赖顺序排列；`P0` 是公开课阻断项，`P1` 是 v0.1 体验完善项，`P2` 为后续版本。

## 当前进度快照（2026-08-29）

代码基线：当前 `master`（含 v0.3 受控联网与按需 Memory 召回）。v0.1 的服务端、学生端、教师端、大屏、场次隔离、流式 Agent 循环和自动化并发验证已经形成闭环；Memory V2 的最小闭环与结构化多 Skill 也已提前实现。当前状态应视为“功能开发基本完成，进入真实环境验收和上线准备”，而不是继续扩展核心功能。

已完成的主要纵向能力：

- [x] 学生多 Tab 对话、删除对话、Markdown 回答与最新设计生效语义。
- [x] Soul / 多 Skill / Memory 分层；Skill 目录渐进披露，正文通过 `load_skill` 按需加载。
- [x] Skill v10 索引：稳定内部 ID、可编辑名称、能力说明、何时使用、`auto/explicit` 触发模式；`explicit` 由服务端按 `@名称` 门控。
- [x] Memory 默认关闭、学生控制、候选/自然写入、跨对话读取、核心画像召回、Skill 加载后按需召回、动态上下文回执和教师场次策略。
- [x] 教师状态墙、课堂锁定、Skill 总开关、Tool 开关、Memory 模式、新建场次与结构化 Skill 查看。
- [x] 投屏快照、历史对话、SSE 重连、DOM 渲染确认和 `delivered` 回执的自动化链路。
- [x] 50 学生 fake LLM 单轮并发基线、race、vet、前端测试、发布构建及干净目录启动验证。

上线前剩余关键路径，按优先级排序：

1. [ ] `P0` 教师 Pad + 教室电脑真机复验投屏，记录浏览器、网络、响应、SSE 和 1 秒切换结果。
2. [ ] `P0` 目标教室 Wi-Fi 下用 10～15 台真实 Pad 跑完整流程。
3. [ ] `P0` 执行 50 学生 30 分钟持续压测，并做小规模真实 DeepSeek API 验证。
4. [ ] `P0` 核对实际反向代理的流式缓冲、压缩和超时配置，补充分段时延观测。
5. [ ] `P1` 收口离线/超时/预算耗尽/被顶下线提示、基础无障碍和 50 人教师墙筛选。
6. [ ] `P1` 完成上线备份恢复演练、匿名运行指标以及修改类接口的 Origin/CSRF 防护。

## 0. 范围与完成标准

### v0.1 范围

- [x] `P0` 学生通过学号登录，同一学号后登录可使旧会话失效。
- [x] `P0` 学生可编辑、保存和刷新恢复 Soul、结构化 Skills、计算器装备和最大步数。
- [x] `P0` 学生可与 Agent 流式对话，并看到易懂的工具“行动记录”。
- [x] `P0` Agent 可自主决定是否调用计算器，并在受限步数内完成工具调用循环。
- [x] `P0` 教师可登录、查看实时状态墙、查看学生设计与对话、锁定/解锁全班、投屏指定学生；投屏自动化链路与渲染确认已通过。
- [ ] `P0` 教师 Pad + 教室电脑完成投屏真机复验；这是教师/大屏闭环最后的设备验收门禁。
- [x] `P0` 学生可创建和切换多个独立对话；新对话立即使用最新设计，且不携带旧对话上下文。
- [x] `P0` 不同 `run_id` 的学生设计、消息和用量严格隔离；教师可新建并切换场次。
- [x] `P0` 单个 Go 二进制内嵌前端资源，配合一个 SQLite 文件即可运行。
- [x] `P0` 通过 50 个模拟学生并发冒烟基线，无 HTTP 5xx、数据库锁错误或模型并发越界。
- [ ] `P0` 完成 10～15 台真实 Pad 联调和 50 学生 30 分钟持续压测。
- [ ] `P1` 移动端、教师投屏和异常提示达到课堂可用水平。

### 原计划后置能力

- [x] `P1` 长期 Memory 已提前完成最小闭环，并继续按 Memory V2 计划渐进演化。
- [ ] `P2` 导出 zip、随机点名、优秀池轮播放仍放入后续版本。
- [x] `P2` v0.3 已实现受控网页访问，以及智谱、DeepSeek、千问可切换联网搜索。
- [ ] `P2` 绘图和受控文件工具继续放在 v0.3 后续切片。
- [x] `P2` v0.4 已完成远程 Python 沙箱、代码编辑运行、LLM Tool 和执行文件产物的第一版闭环；生产配额、监控、压测和真机验收继续在第 16 节跟踪。
- [ ] `P2` 多智能体演示暂缓到 v0.5 或更后，不占用 Python 执行与文件产物的实施资源。
- [ ] `P2` 不建设学校—班级—课程、复杂审核等长期运营能力；学生侧“新建/切换对话”已根据实测反馈纳入 v0.1。

### 统一验收规则

- [ ] 每个接口至少覆盖成功、未登录、无权限、参数非法和核心冲突路径。
- [ ] 所有数据库读写显式携带 `run_id`，禁止仅按 `student_id` 查询或更新课堂数据。
- [ ] 所有学生输入、模型输出、工具参数和技术 JSON 在前端按纯文本渲染，不写入 `innerHTML`。
- [ ] 日志不得记录 DeepSeek Key、教师口令、完整 session token 或包含姓名的模型请求。
- [ ] 合并前执行 `go test ./...`、`go vet ./...`，并完成一次从空目录启动的人工冒烟测试。

### 2026-08-04 实测反馈（优先处理）

#### A. 学生多对话与设计更新隔离

- [x] `P0` 区分“登录 session”和“聊天 conversation”，为对话增加稳定的 `conversation_id`；消息必须按 `run_id + student_id + conversation_id` 隔离。
- [x] `P0` 学生端增加“新建对话”和对话切换入口；新建不删除旧对话，旧对话仍可查看和继续。
- [x] `P0` 新对话使用创建/发送时的最新 Soul（人设）、Skills 和工具配置，不把旧对话历史带入模型上下文，避免更新设计后仍受旧上下文干扰。
- [x] `P0` 明确已有对话在设计更新后的语义：默认继续对话时采用最新设计，但保留该对话历史；UI 提示“设计已更新，建议新建对话测试”。
- [x] `P0` 补齐创建、列表、切换、消息分页 API，以及跨学生、跨场次、跨对话越权测试。
- [x] `P0` 验收：同一学生先用旧 Skill 对话，修改 Skill 后新建对话；模型请求只包含新设计和新对话消息，旧对话内容不出现。

#### B. 大屏投屏失败（回归缺陷）

> 2026-08-19 进一步增加大屏渲染回执：教师端只有收到大屏 DOM 更新后的确认才显示“大屏已确认显示”；无连接或回执超时返回 `202 delivered:false`。自动化已覆盖快照、广播、SSE、DOM 渲染和回执；教师 Pad + 教室电脑真机复验仍待完成。详见 `doc/投屏链路验证记录.md`。

- [x] `P0` 已记录 2026-08-04 原始故障缺少设备、HTTP、SSE 和控制台证据，无法事后伪造为已复现；真机复验若再次失败，按 `doc/投屏链路验证记录.md` 清单同步留存两端证据。
- [x] `P0` 逐段验证投屏链路：教师按钮 → `POST /api/teacher/spotlight` → 当前投屏快照保存 → 大屏事件广播 → `/api/screen/events` 接收 → DOM 渲染 → `/api/screen/ack` 回执。
- [x] `P0` 修复后增加集成测试，覆盖大屏先连接/后连接、指定回合/默认最新完整回合、无完整回合、SSE 断线重连和切换学生。
- [ ] `P0` 在教师 Pad + 教室电脑组合上复验：点击后 1 秒内切换；失败时教师端不能误报“已推送到大屏”。

#### C. Skill 不应变成强制执行的固定角色

- [x] `P0` 先确认产品语义：Skill 是“满足适用条件时采用的方法/能力”，不是每轮必须执行的固定剧本，也不等同于工具调用。
- [x] `P0` 调整 prompt 分层，明确要求模型先判断当前任务是否匹配 Skill；不匹配时按 Soul 和通用能力正常回答，不得为了展示 Skill 而强行套用。
- [x] `P0` Skill 使用独立的能力说明、何时使用和触发模式；`auto` 支持本轮不适用，`explicit` 只有学生 `@名称` 时才进入目录。
- [x] `P0` 讨论并决定后续是否支持多 Skill（名称、说明/触发条件、正文、启用状态）及按需选择；决定前不把“始终注入一整份 Skill”当作最终形态。决策见 `doc/Skill架构决策.md`。
- [x] `P0` 增加行为测试：目录 prompt 不泄漏正文、匹配任务通过 `load_skill` 加载、未填写或无关任务仍可直接回答、未选择 Tool 不提供定义、伪造调用不执行、`explicit` Skill 未被 `@名称` 点名时不进入目录。

#### D. Soul / Memory / Skill 的概念与展现

- [x] `P0` 在产品文案和界面中清楚呈现 Soul / Skill / Memory / Tools：前三者分别表示身份、适用方法和长期事实，Tools 独立管理可调用能力与 Python 实验台。
- [x] `P0` 明确上下文优先级与边界：平台安全规则 > Soul > 当前任务相关 Skill > 可用 Memory > 当前对话；Memory 只作为可纠正的事实，不得覆盖安全规则或伪装成系统指令。
- [x] `P1` 设计 Memory MVP：自动提取候选记忆、学生确认/查看/编辑/删除/清空、跨对话生效，并明确是否跨课堂场次保留。MVP 仅在同一 `run_id` 内保留，详见 `doc/MemoryMVP设计.md`。
- [x] `P1` 区分“对话历史”和“长期 Memory”：新建对话清空短期上下文，但仅在开启 Memory 时带入经过确认且与当前任务相关的长期记忆。
- [x] `P1` 学生工作台以并列、可解释的方式展示 Soul / Memory / Skills，并在发送或行动记录中提示本轮实际采用了哪些 Skill、读取了哪些 Memory（不得暴露隐藏推理）。
- [x] `P1` 为 Memory 加入条数、长度、总 token、隐私隔离和删除验收；记忆提取失败不得阻断主对话。

#### E. 场次切换与重新登录语义（已确认）

- [x] `P0` 普通服务进程重启不创建新场次；同一活动场次内未过期、未撤销的学生 session 继续有效。
- [x] `P0` 教师“新建场次”相当于切换到新的班级/课堂数据空间：结束旧场次并要求学生重新登录，旧场次数据保留但不带入新场次。
- [x] `P1` 教师端二次确认文案明确区分“重启服务”和“新建场次”，说明只有新建场次会让当前学生重新登录。

#### F. `Ctrl+C` 退出被 SSE 长连接拖慢

- [x] `P0` 为服务建立全局 shutdown context；收到 `SIGINT/SIGTERM` 后先取消学生事件、教师状态墙、大屏 SSE 和在途聊天，再调用 HTTP graceful shutdown。
- [x] `P0` SSE handler 同时监听请求 context 与全局 shutdown context，停机时立即退出并释放订阅者；不得等待 15 秒超时才退出。
- [x] `P0` 检查并记录 `http.Server.Shutdown` 返回错误；首次信号优雅退出，必要时允许第二次 `Ctrl+C` 强制结束。
- [x] `P0` 增加停机测试：保持三类 SSE 连接和一个 fake LLM 请求时触发 shutdown，连接及时关闭、模型请求被取消，进程在 2 秒内结束且无 goroutine 泄漏。

#### G. 对话流式输出颗粒度与视觉反馈

- [x] `P0` DeepSeek 请求已设置 `stream: true`，服务端按上游 SSE delta 解析，并在每个前端 NDJSON 事件后调用 `Flush`；DeepSeek API 本身支持流式传输。
- [ ] `P0` 增加分段时间观测，分别记录匿名化的“上游 delta 到达、服务端 flush、浏览器 read/绘制”时间，定位整段出现发生在模型、代理、网络还是前端渲染层。
- [x] `P0` 增加带延迟 fake upstream 的端到端测试，证明首个 `text_delta` 在整轮完成前到达浏览器，并覆盖多个 delta 被合并在同一个网络 chunk 的情况。（服务端提前 flush 与浏览器 NDJSON 合并 chunk/UTF-8 分片均已有自动化覆盖。）
- [ ] `P0` 核对实际部署代理是否关闭响应缓冲和压缩聚合；保留 `Cache-Control: no-transform`、`X-Accel-Buffering: no`，并完成局域网真机测试。
- [x] `P1` 前端使用短队列配合 `requestAnimationFrame` 渐进绘制合并到达的 delta，避免一整个段落同时跳出；设置队列上限，后台标签页或积压过多时立即追平，不人为拖慢答案完成时间。
- [x] `P0` 显式启用并解析 DeepSeek `reasoning_content`，通过独立的 `reasoning_delta` 事件流式发送；不得与最终正文 `text_delta` 或真实工具行动记录混在一起。
- [x] `P0` 学生端增加可折叠的“模型思考草稿”：生成时显示“正在思考”并允许展开，最终回答开始后默认收起；标注“这是模型生成的草稿，可能不准确”。
- [x] `P0` 思考内容按纯文本渲染并设置单轮长度上限；不得包含密钥、session、管理信息或服务端技术错误，客户端停止生成时同步停止思考流。
- [ ] `P1` 决定思考草稿是否持久化；若持久化，必须支持刷新恢复、分页和删除，且独立于最终回答和 Memory，不把草稿重新作为历史上下文发送给模型。
- [ ] `P1` 教师个体视图可按需展开思考草稿；公开大屏默认不展示，后续如允许展示必须由教师显式开启并经过长度截断。

---

## 1. 工程骨架与配置

- [x] `P0` 初始化 Go module，并建立方案约定的目录：
  - `main.go`
  - `internal/auth/`
  - `internal/agent/`
  - `internal/tools/`
  - `internal/store/`
  - `internal/teacher/`
  - `web/`
  - `data/templates/`
- [x] `P0` 引入最小依赖：`chi` 路由、`modernc.org/sqlite`；其余功能优先使用标准库。
- [x] `P0` 定义集中配置结构并完成校验：
  - 监听地址、公开访问地址；
  - SQLite 路径、学生名单路径；
  - DeepSeek Base URL、模型名、API Key、请求超时、全局并发数；
  - 匿名 `user_id` 使用的独立 HMAC 密钥；
  - 教师口令；
  - session 有效期、Cookie Secure 开关；
  - 每生 token 预算、默认/最小/最大步数、单轮最大工具调用数；
  - Soul、Skill 正文、学生输入和模型输出长度上限。
- [x] `P0` 敏感配置只从环境变量或启动参数读取；提交 `config.example.yaml`，不得提交真实 Key 和口令。
- [x] `P0` 启动时检查必填配置、创建数据目录、打开数据库、执行迁移、导入学生名单、确认当前活动场次。
- [x] `P0` 使用 `embed.FS` 嵌入 `web/` 与内置模板，发布产物不依赖外部前端文件。
- [x] `P1` 增加 `/healthz`；至少返回数据库可用状态和服务版本，不泄漏配置。
- [x] `P1` 增加构建信息（版本、commit、构建时间）和 `Makefile` 的 `build`、`test`、`run` 目标。
- [x] `P1` 编写 `README.md`：本地启动、环境变量、CSV 格式、编译、部署、备份和故障排查。

**验收**

- [x] 未配置教师口令或 DeepSeek Key 时，配置校验失败并给出缺失项；已有单元测试覆盖。
- [x] 已用静态发布二进制、环境配置和 `students.csv` 在干净目录完成首次启动，不依赖目标机 Go 环境；见 `doc/v0.1发布验证记录.md`。
- [ ] 重启服务后数据库内容保留，前端静态资源仍可访问。

---

## 2. SQLite 模型、迁移与名单导入

- [x] `P0` 建立带版本号的迁移机制；基础表之后已逐步迁移 `conversations`、思考/结束原因/未知 usage、Memory、上下文回执、场次能力策略、结构化 Skills 和 v10 Skill 索引元数据。
- [x] `P0` 为实际功能补齐字段和约束：
  - `sessions` 增加 `created_at`、`last_seen_at`、`revoked_at`；
  - `messages` 为 `run_id + student_id + turn_id` 建索引；
  - `designs.max_turns` 添加应用层范围校验；
  - `usage` 各计数列默认 `0`，费用不得为负；
  - 外键开启，删除策略明确，时间统一存 UTC。
- [x] `P0` 约定 `runs.status` 仅允许 `active/ended`；切换场次必须在一个事务中结束旧场次并激活新场次，保证最多一个活动场次。
- [x] `P0` 实现 store 接口及 SQLite 版本，至少包括：
  - 场次创建、获取当前场次、按 ID 获取场次；
  - 学生批量导入、查询；
  - session 创建、校验、刷新活跃时间、撤销、清理过期记录；
  - 设计读取、原子 upsert；
  - 消息按回合写入、按学生分页读取；
  - 用量原子累加和预算检查；
  - 状态墙聚合查询。
- [x] `P0` 实现 `students.csv` 导入：UTF-8、表头 `id,name`，去除首尾空白，拒绝空值和同一场次内重复学号。
- [x] `P0` 明确名单同步规则：启动导入只新增/更新姓名，不删除已有学生及其课堂数据。
- [x] `P1` SQLite 启用 WAL、busy timeout，并限制连接参数以适配并发读写。

**验收**

- [x] 同一学号可存在于不同场次，两个场次的数据互不可见。
- [x] 非法 CSV 不产生半导入数据，并返回具体到行号的错误。
- [x] 并发保存设计、写消息和累计用量测试未出现 `database is locked`；50 学生 fake LLM 基线错误数为 0。
- [x] 服务多次启动不会重复执行迁移或破坏已有数据；已覆盖 v1 历史消息和 v8 单 Skill 升级到当前 schema 的回归测试。

---

## 3. 登录、会话与权限边界

- [x] `P0` 实现 `POST /api/login`：
  - 校验当前活动场次和名单中的学号；
  - 按配置决定是否校验姓名首字；
  - 撤销该 `run_id + student_id` 的旧学生 session；
  - 创建高熵随机 token，并设置 session Cookie；
  - 返回学生姓名、学号和当前场次基本信息。
- [x] `P0` 实现 `POST /api/teacher/login`，使用常量时间比较教师口令，成功后签发教师 session。
- [x] `P0` 编写学生、教师鉴权中间件；教师接口不能被学生 session 访问，学生接口不能跨学号读取数据。
- [x] `P0` Cookie 设置 `HttpOnly`、`SameSite=Lax`、合理 `Path` 和 `Max-Age`；生产 HTTPS 模式设置 `Secure`。
- [x] `P0` 被顶掉、过期或场次已结束的学生 session 返回统一的 `401` 错误码，前端提示重新登录。
- [x] `P0` 每次已鉴权操作节流更新 `last_seen_at`，供状态墙判断“活跃/空闲”。
- [x] `P1` 增加 `POST /api/logout`；学生和教师均只撤销自己的当前 session。
- [ ] `P1` 对登录接口做按 IP 的轻量限速；连续失败只返回通用文案，不暴露名单信息。
- [ ] `P1` 为所有修改类接口校验 `Origin`，降低课堂局域网中的 CSRF 风险。

**验收**

- [ ] 同一学号在第二台设备登录后，第一台设备下一次请求和 SSE 重连均被拒绝并提示被顶下线。
- [ ] 伪造 Cookie、过期 Cookie、学生访问教师接口均得到 `401/403`，无敏感响应体。
- [ ] 姓名不会进入发给 DeepSeek 的消息、`user_id` 或应用日志。

---

## 4. 学生设计、结构化 Skill 与模板

- [x] `P0` 定义设计 API DTO 和校验规则：`persona`、`tools`、`max_turns`；保留 `skill_md` 仅用于旧数据库兼容。
- [x] `P0` 实现 `GET /api/design`：无保存记录时返回带默认值的空设计，不返回 `404`。
- [x] `P0` 实现 `PUT /api/design`：
  - 只允许已注册且由教师显式开放的工具 ID；算术任务统一使用 `python_execute`；
  - 去重工具列表；
  - 限制文本长度和 `max_turns` 范围；
  - 按当前学生和场次原子保存；
  - 保存成功后触发状态墙更新事件。
- [x] `P0` 在 `data/templates/` 提供基础、物理、数学三类预设 Skill；天气探究通过 Skill 指导现有网页工具，不新增 Tool。
- [x] `P0` 实现教师按场次勾选可导入预设；`GET /api/templates` 只返回已开放项，学生导入后得到可编辑副本。
- [x] `P0` 增加结构化 Skill CRUD，按 `run_id + student_id` 隔离，每生最多 12 项；支持名称、能力说明、何时使用、正文、启用状态和触发模式。
- [x] `P0` 数据库 v9 将旧 `designs.skill_md` 迁移为“我的 Skill”，v10 将旧说明迁移为索引元数据并默认使用 `auto`。
- [x] `P0` `explicit` Skill 只有输入出现 `@名称` 时才由服务端加入模型目录；学生端提供“在对话中 @调用”入口。
- [ ] `P1` 用版本号或 `updated_at` 防止同一学生多标签页静默覆盖较新的设计。
- [ ] `P1` 评估将未保存的 Soul/Skill 草稿暂存到浏览器本地，并以防抖方式同步服务端；服务端 SQLite 仍为权威副本。

**验收**

- [x] 保存后刷新页面，Soul、结构化 Skills、工具和最大步数完整恢复；已通过 API、迁移测试和 headless Chromium 验证。
- [x] 伪造未知工具、越界步数或超长文本返回结构化 `400`，不写入部分数据。
- [x] 学生 A 无法通过修改请求参数读取或修改学生 B 的设计或 Skill。

---

## 5. Tool 接口与计算器退役

- [x] `P0` 在 `internal/tools/` 定义统一工具接口：名称、学生可读说明、模型 JSON Schema、参数校验、执行结果。
- [x] `P0` 实现受限数学表达式解析器，不调用 shell、JavaScript、模板执行器或任意代码求值。
- [x] `P0` v0.1 仅支持明确列出的能力：整数/小数、括号、`+ - * /`；如支持幂、百分数或函数，先补充测试和 UI 说明。
- [x] `P0` 拒绝非法字符、除零、过深括号、过长表达式、非有限结果和超大数值。
- [x] `P0` 返回稳定的工具结果结构，包含可展示摘要与提供给模型的纯文本结果。
- [x] `P0` 表驱动测试覆盖运算优先级、负数、小数、括号、非法输入和资源上限。
- [x] `P0` 退役独立计算器：从注册表和学生界面删除，数据库 v13 迁移自动清理旧场次与旧设计中的 `calculator`。

**验收**

- [x] `23*17` 返回 `391`，复杂合法表达式结果正确。
- [x] `../`、函数调用、脚本片段、超长表达式不会被执行，并产生可供 Agent 纠正的工具错误。

---

## 6. DeepSeek 客户端与 Agent 循环

- [x] `P0` 抽象 LLM 客户端接口，生产实现调用 DeepSeek OpenAI 兼容接口，测试使用可脚本化 fake client。
- [x] `P0` 使用服务端密钥；为 DeepSeek `user_id` 生成匿名稳定值，例如截断的 `HMAC-SHA256(run_id + student_id)`，密钥与 session 密钥分离。
- [x] `P0` 组装分层 system prompt：固定课堂安全说明 + Soul + Skill 索引目录 + 本轮相关 Memory；Skill 正文仅在模型调用 `load_skill` 后提供，只注入最终有效的工具定义。
- [x] `P0` 实现 `POST /api/chat` 的完整 Agent 循环：
  1. 校验全班锁定状态、输入长度、学生预算和设计；
  2. 写入 `user` 消息并生成唯一 `turn_id`；
  3. 调用模型；
  4. 若有 tool call，校验名称和参数、推送行动事件、执行工具、保存 `assistant/tool` 记录，再次调用模型；
  5. 若无 tool call，流式推送最终正文并保存 `assistant` 消息；
  6. 收尾累计 token、调用次数和估算费用。
- [x] `P0` 将 `designs.max_turns` 统一解释为“单个学生请求允许的最大 LLM 迭代次数”；另用服务端 `max_tool_calls` 限制一轮工具调用总数。
- [x] `P0` 实现每名学生最多一个进行中的聊天请求；重复请求返回 `409 CHAT_IN_PROGRESS`。
- [x] `P0` 使用全局信号量限制同时进行的 LLM 调用，等待也要响应请求取消。
- [x] `P0` 为每次模型调用设置超时；仅在尚未向浏览器输出任何事件时重试一次。
- [x] `P0` 达到步数、工具次数或预算上限时停止循环，保存明确结束原因并返回学生可理解的提示。
- [x] `P0` 客户端断开时取消上游请求；已产生的消息和用量按实际结果落库，不制造半条成功回答。
- [x] `P0` 兼容一个模型响应包含多个 tool calls 的情况，并按稳定顺序执行和记录。
- [x] `P0` 不向前端暴露模型原始错误、上游响应头或密钥；429、超时、网络错误映射为友好错误码。
- [ ] `P1` 将模型名、token 用量、耗时、结束原因记录为结构化日志和匿名指标。

### 对话流事件协议

- [x] `P0` 在编码前固定事件格式并写入文档，建议使用 NDJSON：每行一个 JSON，至少包含：
  - `turn_start`：`turn_id`；
  - `turn_start` 同时携带实际提供的 Soul 与 Memory 上下文回执；
  - `skill_loaded`：实际加载的 Skill ID、名称和本轮已加载列表；
  - `tool_start`：步骤、工具名、学生可读输入摘要；
  - `tool_result`：步骤、学生可读结果摘要、是否成功；
  - `text_delta`：正文增量；
  - `reasoning_delta`：可折叠的模型思考草稿增量，与正文和工具行动分离；
  - `turn_end`：结束原因和本轮用量；
  - `error`：稳定错误码和友好文案。
- [x] `P0` 原始 tool call JSON 仅放在“技术细节”字段，限制长度并保持纯文本。
- [x] `P0` 为流式解析器编写分片测试：一个 JSON 跨多个网络 chunk、一个 chunk 含多条事件、UTF-8 中文被拆分时均能正确处理。

**验收**

- [x] fake client 覆盖“直接回答”“一次工具调用”“连续多次工具调用”“非法工具”“超限”“上游超时”“客户端中断”。
- [x] 未勾选计算器时模型拿不到其定义；伪造调用也不会执行。
- [x] 全班同时请求时，实际 LLM 并发不超过配置值；同一学生双击发送只产生一个完整回合。
- [ ] 发给 DeepSeek 的请求只含匿名 `user_id`，不含姓名、学号明文或 session token。

---

## 7. 学生工作台

- [x] `P0` 创建 `web/index.html`，实现学号登录页和登录后的单页工作台。
- [x] `P0` 实现左右分栏工作台；左侧以 Soul / Skill / Memory / Tools Tab 分类管理，Tools 收纳工具、最大步数与 Python 实验台，右侧为多对话区。
- [x] `P0` Skill Tab 支持多项列表、改名、能力说明、何时使用、`auto/explicit`、启停、删除和 `@名称` 调用；模板只能创建新草稿，不覆盖当前 Skill。
- [x] `P0` 表单改动后显示未保存状态；保存期间禁用重复提交，成功/失败反馈清晰。
- [x] `P0` 实现对话区：输入、发送、停止生成、正文流式追加、滚动控制。
- [x] `P0` 实现每轮“行动记录”折叠面板；默认显示易懂摘要，二级折叠展示原始技术 JSON。
- [x] `P0` 实现 `GET /api/messages?cursor=...`，只返回当前学生在当前场次的消息，按稳定游标分页。
- [x] `P0` 页面加载时恢复设计和历史对话；历史消息按 `turn_id` 还原工具记录和最终回答。
- [x] `P0` 实现学生端 `GET /api/events` SSE：首次连接收到锁定快照，后续处理锁定/解锁和 session 失效事件。
- [x] `P0` 被锁定时禁用发送和设计修改入口但保留查看；服务端拒绝是最终保障。
- [x] `P0` SSE 断线自动重连；重连后以服务端快照纠正本地状态。
- [x] `P0` 所有动态内容使用 `textContent` 或 DOM Text 节点渲染。
- [x] `P0` Agent 最终回答支持受控 Markdown DOM 渲染；历史消息和实时流结束后的显示一致，不执行原始 HTML。
- [x] `P1` 针对 Pad 做响应式布局，交互控件触控区域不小于约 44px，输入框不会被软键盘完全遮挡。
- [ ] `P1` 增加明确的空状态、离线提示、上游超时提示、被顶下线提示和预算耗尽提示。
- [ ] `P1` 做基础无障碍处理：label、键盘焦点、足够对比度、状态变化可被读屏识别。

**验收**

- [ ] iPad Safari、Android Chrome 和桌面 Chrome 完成“登录→套用模板→保存→对话→查看行动记录→刷新恢复”；桌面 headless Chromium 已通过，真实移动设备仍待验收。
- [ ] 快速双击发送、刷新、断网重连不会产生重复回合或让按钮永久卡死。
- [ ] 输入 `<img onerror=...>`、模型返回 HTML、tool JSON 含 HTML 时页面只显示文本。

---

## 8. 实时事件中心与课堂锁定

- [x] `P0` 实现进程内事件中心，按用途分离学生广播、教师状态墙和大屏事件，慢客户端不得阻塞发布者。
- [x] `P0` SSE 响应设置正确头部、定时心跳、请求取消清理和代理禁用缓冲提示。
- [x] `P0` 每个 SSE 连接先发送当前权威快照，再订阅增量事件，避免连接窗口丢事件。
- [x] `P0` 实现 `GET /api/events`，只允许学生接收当前场次的课堂控制事件。
- [x] `P0` 实现 `POST /api/teacher/lock {"locked": true|false}`：事务更新当前 `runs.locked`，再广播新状态。
- [x] `P0` `/api/chat` 和设计修改接口在服务端读取权威锁定状态，不能只依赖 SSE/前端按钮。
- [ ] `P1` 对事件订阅数设置上限并暴露匿名连接数指标；断开连接及时释放 goroutine 和 channel。
- [x] `P1` 增加 SSE 重连和慢消费者测试，使用 race detector 检查事件中心。

**验收**

- [ ] 教师锁定后，已连接学生在 1 秒内看到按钮禁用；绕过前端直接请求也被拒绝。
- [ ] 100 次连接/断开后无持续增长的订阅者、goroutine 或内存。
- [ ] 服务重启后学生重连，首个快照仍能恢复数据库中的锁定状态。

---

## 9. 教师管理台

- [x] `P0` 创建 `web/teacher.html` 和教师口令登录页。
- [x] `P0` 实现 `GET /api/teacher/wall` SSE，初始快照包含全班学生，增量事件至少由登录、活跃、设计保存、新消息和锁定状态触发。
- [x] `P0` 状态墙每个学生卡片显示：
  - 学号、姓名；
  - 是否登录；
  - Soul 是否填写、是否存在启用的结构化 Skill；
  - 对话轮数（按用户发起的 `turn_id` 计）；
  - 最后活跃状态，超过 3 分钟显示 idle。
- [x] `P0` idle 状态不能只依赖事件；前端定时根据服务端时间与 `last_seen_at` 重算。
- [x] `P0` 实现 `GET /api/teacher/student/{id}`，返回当前场次学生的设计、消息、工具记录和用量；长对话支持分页或游标。
- [x] `P0` 教师个体视图展示结构化 Skill 的能力说明、使用条件和触发模式，不再依赖旧 `skill_md` 文本。
- [x] `P0` 实现个体视图，能从状态墙进入并返回，不丢失实时连接。
- [x] `P0` 顶部常驻全体锁定/解锁控件；显示权威状态并防重复点击。
- [x] `P0` 实现 `POST /api/teacher/run`：校验场次名称、创建并切换新场次、导入/复制名单、广播旧场次结束。
- [x] `P0` 新建场次操作增加二次确认，明确说明不会删除旧场次，但当前学生需重新登录。
- [ ] `P1` 状态墙适配 50 个格子的投屏布局，可按未登录、未保存、活跃状态筛选。
- [ ] `P1` 教师查看个体对话时接收实时增量，不需要刷新整页。

**验收**

- [ ] 50 个学生状态能在一个投屏页面快速识别“来了没、写了没、聊了没、还活着没”。
- [ ] 学生保存设计或完成一轮对话后，教师状态墙在 1 秒内更新。
- [ ] 新建场次后旧场次数据仍在数据库中，新旧场次查询结果严格隔离。

---

## 10. 大屏投屏

- [x] `P0` 注册页面路由：`GET /` 返回学生页，`GET /teacher` 返回教师页，`GET /screen` 返回大屏页；API 路由不得被静态文件 fallback 吞掉。
- [x] `P0` 创建 `web/screen.html`，使用大字号展示学生姓名、设计摘要、选定的一轮对话和行动记录。
- [x] `P0` 实现 `POST /api/teacher/spotlight {"id":"...","turn_id":"..."}`；只允许选择当前场次学生及其回合。
- [x] `P0` 实现 `GET /api/screen/events` SSE；大屏连接后先收到当前投屏快照，再接收切换事件。
- [x] `P0` 投屏内容隐藏学号、token、费用、session、原始错误等管理信息，只保留姓名和教学内容。
- [x] `P0` 明确最佳回合选择方式：教师可指定 `turn_id`；未指定时选该生最新一个已完成回合。
- [x] `P1` 无投屏目标、学生尚无完整回合、内容过长和连接中断时提供适合公开展示的空状态。
- [x] `P1` 支持浏览器全屏下 16:9 显示，并避免长文本撑出屏幕。

**验收**

- [ ] 教师 Pad 点击“投屏展示”后，大屏在 1 秒内切换，无需操作教室电脑。
- [x] 查看页面源代码和网络响应时，公开大屏接口不出现学号及管理用量信息。

---

## 11. 安全、限额、错误处理与可观测性

- [x] `P0` 所有 JSON 请求设置 body 上限，所有字符串字段设置字符上限，拒绝未知的大型字段。
- [x] `P0` 每生默认 token 预算设为 20 万且可配置；预算检查与用量累加避免并发竞态。
- [x] `P0` 明确 token 预算口径：输入 + 输出累计；工具调用次数不冒充 token，用量缺失时记录“未知”而不是零成本。
- [x] `P0` 建立稳定错误码，例如：`UNAUTHENTICATED`、`SESSION_REPLACED`、`CLASS_LOCKED`、`CHAT_IN_PROGRESS`、`BUDGET_EXCEEDED`、`UPSTREAM_TIMEOUT`、`TURN_LIMIT_REACHED`。
- [x] `P0` 捕获 panic 并返回通用错误；流开始后用流内 `error` 事件收尾，不能再尝试改 HTTP 状态码。
- [x] `P0` 记录必要审计事件：教师登录失败、锁定切换、新建场次、投屏切换；不记录敏感值。
- [ ] `P1` 提供匿名运行指标：在线 SSE 数、进行中聊天数、LLM 延迟/错误数、token 总量、预算拒绝数。
- [x] `P1` 增加优雅停机：停止接收新聊天、取消/等待在途请求、关闭 SSE、同步数据库。
- [ ] `P1` 增加数据库备份说明和启动前可选自动备份，备份文件不进入二进制或 Git。

**验收**

- [ ] 上游超时、429、断网、SQLite 短暂繁忙时，前端得到友好提示且服务不崩溃。
- [ ] 运行日志搜索不到 API Key、教师口令和完整 session token。
- [x] 预算耗尽或课堂锁定后，不再发起新的 DeepSeek 请求。（模型并发槽获取后会再次检查权威场次状态，锁定前已排队的请求不会继续调用上游。）

---

## 12. 自动化测试与发布

- [x] `P0` 单元测试：配置、CSV、session、设计校验、计算器、prompt 组装、Agent 状态机、事件中心。
- [x] `P0` 集成测试：使用临时 SQLite + fake LLM 跑通全部 v0.1 API，不依赖真实 DeepSeek。
- [x] `P0` 权限测试：学生横向越权、学生访问教师接口、旧场次 session、被撤销 session、公开大屏字段泄漏。
- [x] `P0` 并发测试：同生双发、50 生同时聊天、保存与状态墙并发、锁定与发消息竞态、新建场次竞态。
- [x] `P0` 流式测试：慢客户端、中途断开、SSE 重连、代理缓冲、中文 UTF-8 分片。
- [x] `P0` 执行 `go test -race ./...`，修复所有数据竞争。
- [x] `P0` 编写 50 个模拟学生的压测/冒烟脚本，覆盖登录、保存、聊天、锁定和 SSE 重连；默认使用 fake LLM，另提供小规模真实 API 模式。
- [x] `P0` 记录压测结果：机器规格、并发、P50/P95 延迟、错误率、最大 goroutine、内存、数据库锁错误和模型调用数。见 `doc/50学生冒烟测试记录.md`。
- [x] `P0` 编译目标平台二进制，生成校验和；用干净目录验证首次启动。见 `doc/v0.1发布验证记录.md`。
- [x] `P1` 增加 CI：格式检查、测试、vet、race（可按平台拆分）、构建产物。
- [x] `P1` 增加 HTTPS 反向代理部署示例，并确认 SSE 和流式 POST 禁用缓冲、超时足够长。见 `deploy/nginx.conf.example` 与 `doc/HTTPS反向代理部署.md`。

### v0.1 发布门禁

- [ ] 所有 `P0` 项完成。
- [x] 当前自动化测试全部通过：Go 单元/集成/并发测试、race、vet 和 `web/*.test.js`；是否存在发布阻断问题仍以真机验收为准。
- [ ] 50 个模拟学生连续运行 30 分钟，HTTP 5xx 为 0（人为注入上游故障除外）。
- [ ] 10～15 台真实 Pad 在目标教室 Wi-Fi 完成全流程。
- [ ] 锁定广播和投屏切换 P95 小于 1 秒。
- [ ] 实际 DeepSeek 小流量测试验证工具调用格式、流式解析、token 用量字段和模型名配置。

---

## 13. 公开课上线清单

### 提前 3 天

- [ ] 用正式候选二进制完成 50 学生压测并归档结果。
- [ ] 在上课教室用 10～15 台真实 Pad 联调 Wi-Fi、登录、流式对话、SSE 重连和锁定。
- [ ] 验证域名、HTTPS 证书和教室网络可访问性。
- [ ] 修复后冻结版本，只接受阻断课堂的问题。

### 提前 1 天

- [ ] 备份数据库，验证恢复步骤。
- [ ] 检查 DeepSeek 余额、模型名、API Key、限额和服务可用性。
- [ ] 校验正式 `students.csv` 的人数、学号和姓名；抽查 5 人登录。
- [ ] 创建正式场次并检查所有学生初始状态为空。
- [ ] 修改教师口令，确认配置文件权限和日志无敏感信息。
- [ ] 准备本地录屏、预跑对话截图和纸质任务卡作为断网兜底。

### 当天课前

- [ ] 启动服务并检查 `/healthz`、磁盘空间、系统时间和数据库可写。
- [ ] 教师端登录，执行一次锁定/解锁。
- [ ] 大屏打开 `/screen` 并全屏，执行一次投屏切换。
- [ ] 使用测试学生完成“登录→保存→计算器工具调用→行动记录”。
- [ ] 清理测试数据或重新创建正式空场次。
- [ ] 确认纸质任务卡和离线兜底文件可在无网络条件下打开。

### 课后

- [ ] 结束/锁定当前场次，防止学生继续产生费用。
- [ ] 复制 SQLite 文件及日志到按日期命名的归档目录。
- [ ] v0.2 导出功能完成前，用只读备份代替“一键导出 zip”。
- [ ] 记录故障、延迟、学生误操作和下一版改进项。

---

## 14. v0.2（推荐，v0.1 稳定后）

- [x] `P1` 已提前完成记忆数据模型、开关和“AI 记住了我什么”查看/删除能力。
- [x] `P1` 已实现 `GET /api/memory`，记忆按 `run_id + student_id` 隔离并可由学生纠正或清空。
- [x] `P1` 记忆提取失败不影响主对话；已限制条数、单条长度和总 token。
- [x] `P1` 教师可按场次选择关闭、确认后记忆或自然记忆，并控制允许提供的 Tool；策略通过 SSE 同步，服务端按 revision 防止旧异步任务越权写回。
- [x] `P1` 自然模式下现有提取器产生的候选可自动写入，并提供非阻塞提示和撤销；上下文回执只显示本轮实际注入的 Memory。确定性敏感分类仍列为下一项。
- [x] `P1` 读取端已从简单词项相交演进为有限候选评分；称呼/姓名等核心画像稳定召回，普通内容按相关度排序，每轮最多 5 条并受 token 上限约束。
- [x] `P1` 多 Skill 已提前完成：目录渐进披露、正文按需加载、实际使用回执、结构化索引和 `auto/explicit` 触发模式。
- [ ] `P1` Memory 提取升级为结构化提案，增加类型、置信度、敏感度、证据与确定性敏感信息拒绝规则。
- [ ] `P1` 实现 Memory 重复合并、冲突纠正、supersede/forget、`last_used_at/use_count` 和变更审计。
- [ ] `P2` 用匿名课堂样本评估本地评分、小模型重排和 embedding；只有离线评测显示收益时才引入语义索引。
- [ ] `P2` 增加带 TTL 的近期经历摘要，保持它与长期学生画像、Soul 和 Skill 分离。
- [ ] `P2` 增加教师随机点名：候选范围仅为当前场次已保存设计的学生，可避免短时间重复。
- [ ] `P2` 增加优秀池持久化、加入/移除接口和大屏自动轮播；没有作品时安全停止。
- [ ] `P2` 实现 `GET /api/teacher/export?run_id=...`：导出名单、设计、对话、行动记录、用量和清单文件。
- [ ] `P2` 导出使用流式 zip、文件名净化和大小上限；导出期间不阻塞课堂写入。
- [ ] `P2` 补充记忆隐私、随机点名、轮播重连和导出完整性测试。

---

## 15. v0.3（彩蛋工具）

- [ ] `P2` 补全统一工具限额：搜索和网页读取已限制为每轮各 2 次；图片张数、文件大小、每场搜索次数和总成本仍待实现。
- [x] `P2` 实现受控网页访问工具 `web_fetch`：仅公开 HTTP/HTTPS、固定端口、DNS 解析与实际拨号双重阻断内网/本机地址，限制重定向、内容类型、响应大小、超时和文本长度；注册后由教师按场次显式开放。
- [x] `P2` 实现可切换联网搜索：智谱 `web_search` 使用固定官方 HTTPS 端点、最多 5 条结果、20 QPS/20 并发保护、12 秒超时、256 KiB 响应上限、429 单次退避、5 分钟缓存与同查询合并；DeepSeek 搜索作为独立工具子请求，可由教师按场次在 Anthropic Messages 与 Responses 通道间切换；千问搜索使用百炼 Responses API 的内置 `web_search`，保留检索词与来源，并继续由本地 `web_fetch` 受控读取正文。
- [ ] `P2` 实现绘图工具，接入第三方服务；图片保存到按场次和学生隔离的受控目录。
- [ ] `P2` 实现受控文件工具；规范化路径并验证最终路径位于学生目录内，禁止绝对路径和 `..` 越界。
- [ ] `P2` 教师端显示搜索、图片和费用用量，并支持分项熔断。
- [ ] `P2` 将 `web_fetch` 已实现的内容类型、响应大小、重定向、内网地址和超时防护抽成所有外部工具共用边界。
- [ ] `P2` 压测多工具串联，验证 `max_turns`、`max_tool_calls` 和成本上限能可靠终止循环。

---

## 16. v0.4（Python 执行与文件产物）

### 16.1 内网 Docker Runner 与 Agent Tool

**第一版边界**

- [x] `P2` Python 执行采用独立内网机器：Runner 本身运行在容器中并挂载该机器的 Docker socket，通过宿主 Docker 创建一次性兄弟容器；课堂 Agent 只调用受限 Runner API，绝不接触 Docker socket。
- [x] `P2` 同时支持两条明确链路：LLM 按需调用 `python_execute`，以及学生在代码编辑器中手动点击运行；手动运行不经过 LLM，只有学生明确要求讲解/调试时才把代码和最近结果加入对话。
- [x] `P2` MVP 只支持固定 Python 运行镜像、固定预装的少量数据处理包、单文件程序和可选 stdin；不支持联网、运行时 `pip install`、任意镜像/命令/环境变量、用户指定的宿主目录挂载、多文件工程或长期运行任务。正式部署仍需将镜像标签固化为审核后的 digest。
- [x] `P2` 输入和执行产物保留在 Runner 的受控持久目录中并设置 TTL；Python 只能读取本次 `/workspace/input`、写入本次 `/workspace/output`，执行完成、超时、取消或 Runner 重启后均有确定的容器和临时状态清理路径。

**Runner 协议与隔离**

- [x] `P2` 固定同步 Runner API：提供受限文件上传/下载接口；执行请求只接受 `request_id`、`code`、`stdin` 和 `input_artifact_ids`，返回 `execution_id`、状态、退出码、stdout、stderr、耗时、截断标记和输出 artifact 元数据。镜像、命令、资源限制和网络策略全部由 Runner 决定。
- [x] `P2` MVP 由请求 context 和 Runner 硬超时终止执行，不先建设任务调度系统；学生停止或 Agent 断开时传播取消，Runner 在连接消失后杀掉对应容器；课堂中途锁定对已开始执行的强制取消仍待补充。
- [x] `P2` Runner 支持独立高熵 Bearer Token，部署文档要求仅绑定/放行内网 Agent 来源且不与其他密钥复用；HTTPS/mTLS 在网络边界扩大时再增加。
- [ ] `P2` Runner 使用固定 digest 的预拉取镜像和一次性非 root 容器；默认 `network=none`、只读根文件系统、全部 capability 删除、`no-new-privileges`，并限制 CPU、内存、swap、PID、临时磁盘、进程数和最大执行时间。
- [x] `P2` 只有 Runner 容器挂载执行机 Docker socket，Python 容器永远不能看到 socket、宿主目录或 Runner 凭据；执行机不部署 Agent、SQLite 或其他重要服务，并按可随时重装的专用节点管理。
- [x] `P2` 执行机固定挂载 `/var/lib/classroom-runner` 到 Runner 容器内相同绝对路径；Runner 为每次执行生成不可猜测的独立 input/output 子目录，宿主 Docker 只把这两个子目录分别以只读/可写方式挂入对应 Python 容器，所有路径均由 Runner 生成。
- [x] `P2` MVP 使用执行机现有 Docker daemon，不以 rootless Docker、gVisor `runsc` 或额外执行节点作为上线前置条件；这些作为威胁模型或规模变化后的加固项。
- [ ] `P2` Runner 设置独立并发队列和每生/每场配额；过载返回稳定的 busy/retry-after 结果，不与 LLM 并发信号量共用，也不能拖垮普通聊天。
- [x] `P2` 限制 code/stdin/stdout/stderr 长度；stdout/stderr 始终视为不可信数据，不解析为平台指令、HTML 或新的工具调用。
- [x] `P2` Runner 默认不记录代码、stdin、stdout、stderr 或学生身份明文；所有执行容器使用固定 label，Runner 启动时清理上次崩溃留下的孤儿容器。资源用量指标仍待补充。

**Agent 接入**

- [x] `P2` 在 `internal/tools/` 实现远程 `python_execute` Tool，JSON Schema 只向模型开放 `code`、可选 `stdin` 和当前学生已拥有的 `input_artifact_ids`；Agent 必须重新校验每个文件都属于当前 `run_id + student_id` 后才可转交 Runner。
- [x] `P2` Runner 客户端使用独立 HTTP client、总超时、流式上传、下载大小上限和受控错误文案；Agent 不把 Runner Bearer Token、内网地址或原始错误暴露给浏览器和模型。更细的错误类型映射仍待补充。
- [x] `P2` 为工具执行增加独立于 LLM 调用的 context 超时；Runner 侧仍保留更强的硬超时，避免客户端断开成为唯一终止机制。
- [x] `P2` 注册 `python_execute` 并加入教师场次 Tool 开关、学生装备状态和动态权限复查；每次对话最多调用 5 次，并纳入 `max_tool_calls` 总上限。
- [x] `P2` 工具返回给模型的内容只包含状态、退出码、受限 stdout/stderr 和 artifact 摘要；系统规则明确工具输出是不可信数据，成功执行不等于程序逻辑正确。
- [x] `P2` 为 Python 调用生成短的学生可读摘要，不在行动列表中直接铺开完整代码；代码只进入按需展开的纯文本技术详情。
- [ ] `P2` Runner 不可用、认证失败、超时、过载、容器退出和输出截断分别映射为稳定错误；Python 能力失败时普通聊天、Memory、Skill 和其他 Tool 仍可继续使用。

**执行文件产物（Artifacts）**

- [x] `P2` 扩展工具结果和流事件以携带 artifact 元数据：稳定 ID、文件名、MIME、大小、SHA-256、预览能力和过期时间；二进制内容不得塞入模型上下文或 NDJSON 事件。
- [x] `P2` 增加 artifacts 数据表，至少保存 Runner artifact ID，并关联 `run_id + student_id + conversation_id + turn_id + execution_id`；Agent 数据库只保存归属和展示元数据，不保存大文件正文。
- [x] `P2` Agent 提供学生鉴权的上传接口并把文件流式转发给 Runner；Runner 返回 artifact ID 后才写入归属记录。失败时清理 Runner 上的孤儿文件，禁止客户端自行声明另一个学生的 Runner artifact ID。
- [x] `P2` 代码写入 Runner 生成的只读 `main.py` 并通过固定的 `python -I -B /workspace/source/main.py` 执行，从而保留 stdin 给程序的 `input()`；输入 artifact 只读挂载到 `/workspace/input`，输出目录单独可写挂载到 `/workspace/output`，不暴露 artifact 根目录。
- [ ] `P2` Runner 完成后校验 `/workspace/output`：拒绝绝对/越界路径、符号链接、设备文件、命名管道、超量文件和压缩炸弹；MVP 限制文件数量、单文件大小和总大小，并在配置中给出硬上限。
- [x] `P2` 校验后的输入和输出文件正文留在 Runner；Agent 保存 Runner artifact ID、文件名、MIME、大小、SHA-256 和过期时间，并通过自己的鉴权下载接口从 Runner 流式代理文件，浏览器不得直接访问 Runner 地址或持有 Runner Token。
- [ ] `P2` 学生只能上传和访问自己的当前场次文件，教师访问需走教师权限，大屏默认只显示安全的产物摘要；Runner 只信任 Agent，不持有学生登录态，也不自行决定课堂文件归属。
- [ ] `P2` 第一版允许下载 `txt/csv/json/png/jpeg/pdf/xlsx`；图片可受控预览，HTML/SVG 等主动内容默认拒绝或强制附件下载，并设置 `nosniff` 和安全的 `Content-Disposition`。
- [ ] `P2` 小型文本/CSV 只截取有限预览回填给模型；图片、PDF、Excel 等仅提供元数据和下载入口，除非后续明确增加受控解析或多模态能力。
- [ ] `P2` Runner 对输入和输出统一设置 TTL、单生配额和全盘高水位；课堂结束后默认保留 24 小时再清理。Agent 遇到 Runner 返回已过期时同步更新元数据，页面显示“文件已清理”，不得无限重试或误删其他学生文件。
- [x] `P2` MVP 接受 Runner 重装或磁盘损坏后历史文件不可恢复；长期归档、对象存储和“教师收藏后永久保存”作为后续能力，不阻塞临时公开课版本。

**学生与教师界面**

- [x] `P2` 学生端增加适配 Pad 的 Python 编辑器、运行/停止、stdin、stdout/stderr、退出状态和产物列表；界面明确代码草稿和文字结果仅保留在本页，文件按 Runner TTL 保存。
- [x] `P2` 手动运行使用独立 API 和并发状态，不制造虚假的 LLM 回合；提供“让 Agent 解释/调试本次代码与结果”的显式入口，并在聊天输入框展示将要发送给模型的内容。
- [x] `P2` 历史对话刷新后恢复 LLM 发起的 Python 行动和当前对话 artifact 下载入口；已过期文件显示“文件已清理”。
- [ ] `P2` 教师端可查看 Python 是否开放、Runner 健康、排队/运行数、失败率和资源用量，并可独立熔断 Python；不向教师墙广播代码正文或 stdout/stderr。

**测试与验收**

- [ ] `P2` 使用 fake Runner 覆盖：LLM 不调用、单次调用、修正后第二次调用、非法参数、权限中途关闭、超时、取消、过载、Runner 离线、输出截断和 artifact 返回。
- [ ] `P2` 安全测试覆盖：死循环、递归创建进程、内存耗尽、超大输出、路径穿越、符号链接、读取环境变量、探测内网、写根文件系统和伪造指令输出；不得读取 Agent/Runner 密钥或影响其他执行。
- [ ] `P2` 隔离测试证明学生 A 无法读取学生 B 的代码、结果或文件；跨场次、跨对话和过期 artifact 下载均被拒绝。
- [ ] `P2` 完成 50 学生混合压测：聊天、Python 排队、取消、文件下载和课堂锁定同时发生时，Runner 并发不越界，Agent 无 goroutine/连接泄漏，SQLite 无锁错误。
- [ ] `P2` 在真实 iPad Safari、Android Chrome 和桌面 Chrome 验收编辑、运行、停止、错误定位、图片预览、文件下载、刷新恢复及 Runner 断线提示。

**MVP 实施顺序**

1. [x] 定义 Runner 执行、文件上传/下载、错误码和 artifact 清单，并实现 fake Runner 与 Agent 侧客户端测试。
2. [x] 实现单机 Runner 容器、持久 artifact 目录及固定 Docker 执行生命周期：准备本次 input/output、创建、传入 stdin、等待/超时、收集输出、登记文件、删除容器，并覆盖孤儿清理。
3. [x] 接入 `python_execute` Tool、配置、课堂开关、每轮次数限制和不可信工具输出规则，完成工具执行与流事件回环。
4. [ ] 增加 artifact 元数据表、Agent 鉴权上传/下载代理、Runner TTL 与磁盘配额，再接通已有文件作为下一次执行输入的链路。
5. [ ] 增加学生 Python 编辑/运行界面、结果与文件卡片、“让 Agent 解释”入口，最后完成混合并发和真实 Pad 验收。

---

## 17. v0.5 或更后（多智能体演示，暂缓）

- [ ] `P2` 仅提供教师预置的“编排者 + 三专才”演示，不开放学生编辑角色和系统工具。
- [ ] `P2` 明确演示任务、消息协议、最大总步数、每个子 Agent 的预算和整体超时。
- [ ] `P2` 大屏用可视化时间线展示任务拆分、并行/串行执行和结果汇总。
- [ ] `P2` 演示失败时可一键切换到预录结果，不影响 v0.1 学生课堂主流程。

---

## 18. 建议实施顺序（5～7 个工作日）

- [x] **第 1 天：** 工程骨架、配置、迁移、名单导入、场次和 store 测试。
- [x] **第 2 天：** 学生/教师登录、session 顶替、设计 CRUD、三套模板。
- [x] **第 3 天：** 计算器、DeepSeek 客户端、Agent 循环、流事件协议和 fake LLM 测试。
- [x] **第 4 天：** 学生工作台、历史恢复、行动记录、学生 SSE 和全体锁定。
- [x] **第 5 天：** 教师状态墙、个体视图、大屏投屏、新建场次。
- [x] **第 6 天：** 安全边界、异常处理、并发/race/模拟压测和响应式适配；真实移动设备验收仍属于第 7 天门禁。
- [ ] **第 7 天：** 真实 Pad 与真实 DeepSeek 联调、部署演练、断网兜底和版本冻结。

> 若工期不足，优先保证第 1～5 天的 `P0` 闭环，再完成并发与真实设备门禁；不得用删除服务端权限校验、预算限制或场次隔离来换进度。
